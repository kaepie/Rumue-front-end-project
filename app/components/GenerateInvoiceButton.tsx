export default function GenerateInvoiceButton(){

    return (
        <button 
        className = "bg-generateButton text-white rounded-3xl p-3 border-2 border-primaryBackground hover:bg-primaryBackground hover:text-generateButton hover:border-2 hover:border-generateButton">
            Generate Invoice
        </button>
    );
}
